# ============================================
# OpenAI API Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Available models:
# - gpt-4 (most capable, slower, expensive)
# - gpt-4-turbo (faster, cheaper than gpt-4)
# - gpt-4o (optimized, balanced)
# - gpt-4o-mini (fastest, cheapest)
# - gpt-3.5-turbo (fast, cheap, less capable)
OPENAI_MODEL=gpt-4

# ============================================
# Flask Server Configuration
# ============================================
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_PORT=5000

# ============================================
# CORS Settings
# ============================================
# Comma-separated list of allowed origins for your Angular frontend
CORS_ORIGINS=http://localhost:4200,http://localhost:3000

# ============================================
# Proxy Settings (Optional)
# ============================================
# Uncomment if you're behind a corporate proxy
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=https://proxy.company.com:8080

# ============================================
# Playwright Configuration (Optional)
# ============================================
# Browser to use (chromium, firefox, webkit)
PLAYWRIGHT_BROWSER=chromium

# Headless mode (true/false)
PLAYWRIGHT_HEADLESS=true

# Timeout for page load (milliseconds)
PLAYWRIGHT_TIMEOUT=30000